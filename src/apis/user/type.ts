export type LoginResult = {
  token: string;
};
export type userProfile = {
  email: string;
  name: string;
  createdAt: string;
  updatedAt: string;
  _id: string;
  profileimg: string;
  view: string[];
};

export type UserInfo = {
  userId: string;
  userName: string;
  profileimg: string;
};
